dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install -y virtualbox; sudo apt-get install -y vagrant; sudo apt-get install -y virtualbox-dkms; 
  post:
    - vagrant up --provision && VBoxManage --version
test:
  override:
    - curl http://0.0.0.0:8080
